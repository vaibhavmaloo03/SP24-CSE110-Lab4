1. The use of "let" instead of "var" restricts the variables' accessibility outside their block (parentheses) scope. Similarly, num1 and num2 were not configured to strictly hold or convert values to integers.

2. file is attached